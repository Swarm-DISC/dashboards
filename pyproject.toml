[project]
name = "vires-dashboards"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "viresclient",
    "panel",
    "param",
    "nbconvert",
]
